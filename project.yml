parameters: {}
packages:
    - name: default
      shared: false
      parameters: {}
      annotations: {}
      functions:
        - name: "waiting-room"
          binary: false
          main: ""
          runtime: python:3.11
          web: "raw"
          environment:
            REDIS_LOCATION: ${REDIS_LOCATION}
            WAITING_ROOM_SECRET: ${WAITING_ROOM_SECRET}
            WAITING_ROOM_MAX_USERS: "${WAITING_ROOM_MAX_USERS}"
            WAITING_ROOM_BLOCKED_UNTIL: "${WAITING_ROOM_BLOCKED_UNTIL}"
            WAITING_ROOM_BLOCK_DURATION_MINUTES: "${WAITING_ROOM_BLOCK_DURATION_MINUTES}"
          parameters: {}
          annotations: {}
          limits: {}
